## v0.2.3

### English

#### Fixed
- Restore the GitHub Actions workflow so pushes to `main` and version tags trigger CI, cross-platform release builds, artifact uploads, and GitHub Release publishing again.
- Stop ignoring the `.github/` directory. Workflow files are repository configuration and must stay tracked.

#### Changed
- Update the English and Chinese version badges to `0.2.3`.
- Bump the package version to `0.2.3`.

#### Notes
- This release repairs release automation for the `v0.2.2` runtime changes. The visible match output and real-time result-file flushing fixes remain unchanged from `v0.2.2`.

#### Verification
- `cargo fmt --check`: passed.
- `cargo test`: passed, 97 tests.
- `cargo build --release`: passed.
- `git diff --check`: passed.

### 中文

#### 修复
- 恢复 GitHub Actions workflow，使推送到 `main` 和版本 tag 时重新触发 CI、跨平台 release 编译、产物上传和 GitHub Release 发布。
- 不再 ignore `.github/` 目录。workflow 文件属于仓库配置，必须保持跟踪。

#### 变更
- 将中英文版本徽章更新为 `0.2.3`。
- 将包版本提升到 `0.2.3`。

#### 说明
- 本版本用于修复 `v0.2.2` 运行时改动的发布自动化。`v0.2.2` 中的可见命中输出和结果文件实时 flush 修复保持不变。

#### 验证
- `cargo fmt --check`：通过。
- `cargo test`：通过，97 个测试。
- `cargo build --release`：通过。
- `git diff --check`：通过。

### Assets
- Linux: rfuzz-linux.tar.gz
- macOS: rfuzz-macos.tar.gz
- Windows: rfuzz-windows.zip

### Changelog
- Full Changelog: https://github.com/k1115h0t/Rfuzz/compare/v0.2.2...v0.2.3
